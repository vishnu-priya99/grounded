"""Ingestion: turn any supported file into a normalized DocModel, then into
retrievable Chunk objects and/or SQL tables.

Import submodules directly (``grounded.ingest.pipeline``) to avoid import cycles.
"""
